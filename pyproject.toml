[project]
name = "repository_root"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openpyxl",
    "pandas",
    "pdfplumber",
    "psycopg2-binary",
    "PyYAML",
    "pytest",
    "xlrd",
]
